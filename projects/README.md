# Projects

Each Power BI engagement lives in its own folder.

```bash
cp -R projects/_template projects/my-report-name
```

Then open Cursor and run `/pbi-delivery` with that project name.

See `projects/_template/README.md` and `docs/pbi-delivery-kit/README.md`.
